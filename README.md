## 📝 ToDo

- [x] Настроить окружение (socat, виртуальные порты)
- [x] Реализовать базовый протокол Modbus RTU
- [x] Добавить отправку команд на открытие/закрытие (Контроллер)
- [x] Реализовать обработку входящих команд (Шлагбаум)
- [x] Добавить проверку контрольной суммы CRC (Контроллер)
- [x] Реализовать простой UI tkinter (Шлагбаум)
- [ ] Добавить отправку команды о состоянии (Контроллер)


## 🛠 Настройка окружения (Виртуальные COM-порты)

Для запуска проекта необходимо создать эмуляцию соединения между **Контроллером (Master)** и **Шлагбаумом (Slave)**.

### 1. Установка утилиты
Установите `socat` - утилиту для соединения потоков данных:

```bash
brew install socat
```

### 2. Создание портов
Выполните следующую команду в терминале:

```bash
socat -d -d pty,raw,echo=0 pty,raw,echo=0
```

### 3. Подключение
После запуска в терминале отобразятся два адреса. Используйте их для настройки соединения:
- `/dev/ttys417` - **Master** (Контроллер C++)
- `/dev/ttys418` - **Slave** (Шлагбаум Python)

> **Важно:** Не закрывайте окно терминала с запущенной командой `socat`, иначе виртуальные порты перестанут существовать.

## 📡 Протокол взаимодействия

Взаимодействие между компонентами системы осуществляется по протоколу **Modbus RTU**.

*   **Контроллер (Master):** Инициирует обмен данными. Отправляет команды на открытие/закрытие и опрашивает состояние.
*   **Шлагбаум (Slave):** Слушает линию, выполняет команды контроллера и возвращает текущий статус.

Обмен пакетами происходит через созданные ранее виртуальные COM-порты.
